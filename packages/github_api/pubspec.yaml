name: github_api
description: GitHub REST and OAuth Device Flow client used by GitScholar.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  crypto: ^3.0.0
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
