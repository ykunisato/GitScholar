name: scholar_agent
description: Claude Messages API streaming client and tool-use agent loop for GitScholar.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
